# ForgeP2P App Registry
# Catalog of all apps built in this repo.
# The agent updates this when planning or completing an app.

[[apps]]
name = "echo-gossip"
description = "Peers join a gossip topic, broadcast messages, and echo back whatever they receive"
status = "planned"
branch = "dev/echo-gossip"
pattern = "gossip"
steps_total = 5
steps_done = 0
created = "2026-04-12"
