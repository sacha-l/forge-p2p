# ForgeP2P App Registry
# Catalog of all apps built in this repo.
# The agent updates this when planning or completing an app.

# [[apps]]
# name = "echo-gossip"
# description = "Peers echo gossip messages back to the network"
# status = "complete"
# branch = "dev/echo-gossip"
# pattern = "gossip"
# steps_total = 5
# steps_done = 5
# created = "2026-04-12"

[[apps]]
name = "sovereign-notes"
description = "CLI note-taking tool that syncs notes across devices peer-to-peer"
status = "planned"
branch = "dev/sovereign-notes"
pattern = "replication+gossip+rpc"
steps_total = 8
steps_done = 0
created = "2026-04-12"
