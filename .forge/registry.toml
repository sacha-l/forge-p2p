# ForgeP2P App Registry
# Catalog of all apps built in this repo.
# The agent updates this when planning or completing an app.

[[apps]]
name = "mesh-chat"
description = "Two-peer gossip chat between Al and Bobby, visualized in forge-ui"
status = "in-progress"
branch = "dev/mesh-chat"
pattern = "gossip"
steps_total = 6
steps_done = 4
created = "2026-04-13"
